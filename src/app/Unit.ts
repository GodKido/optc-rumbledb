import { Class, Style } from "src/assets/Enums";

export interface Unit {
    // public constructor(
        id: number;
        icon: any;
        name: string;
        class: Class[];
        style: Style
    // ) {}
}